
import 'package:flutter/material.dart';

class LatterColor {

  /// 主色（Primary）：Medium Sea Green
  static const Color primary = Color(0xFF3CB371);

  /// 次主色：Sea Green
  static const Color secondary = Color(0xFF2E8B57);

  /// 点缀色：Pale Green
  static const Color accent = Color(0xFF98FB98);

  /// 蓝绿过渡色：Light Sea Green
  static const Color tealish = Color(0xFF20B2AA);

  /// 浅背景色：淡绿色背景
  static const Color lightBackground = Color(0xFFE6F9F0);

  /// 暗色背景（用于暗色模式可选）
  static const Color darkBackground = Color(0xFF1D4030);

  /// 字体主色（适用于深色背景）
  static const Color textOnDark = Colors.white;

  /// 字体主色（适用于浅色背景）
  static const Color textOnLight = Color(0xFF222222);

  /// 灰色文字（辅助信息）
  static const Color textHint = Color(0xFF888888);

  /// 白色
  static const Color white = Color(0xFFffffff);

  /// 深灰
  static const Color dartGray = Color(0xFF5D6D7E);


}
